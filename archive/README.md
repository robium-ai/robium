# archive/

Frozen copies of prior skill versions. Before the first edit of any version
bump, the skill's current directory is copied to `archive/<name>/<old-version>/`
so the catalog's evolution stays browsable. Never edit archived copies; never
load them as skills (plugin discovery only scans `skills/`).
