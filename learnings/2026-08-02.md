# 2026-08-02 — acceptance run A (backlog consolidation)

First real consolidation pass of the learning engine: 38 queue flags + the 11
of 21 archived transcripts that carried flags + the 12 dated
`learnings/*.md` files reviewed. All 12 dated files already carry
`<!-- absorbed -->` markers covering their full content (skill-updater
passes on 2026-07-11..07-31) — dedup-checked only, nothing re-promoted from
them. Entries below are the flags that cleared the noise bar; see
`.superpowers/sdd/2026-08-02-learning-engine-phase2b-absorb/task-10-report.md`
for the full inventory, the discard list with reasons, and the
correction log for this file's 01/02 entries (originally mis-signed
`[architect] no-skill-fired`; the transcripts themselves disconfirm a
trigger-surface problem — see below).

- [none] no-skill-fired <!-- id: lrn-0802-01 -->
  symptom: session opens with "we'll create a new application. I have
  turtlebot3 robot real robot, want to get it working and want to have a
  teleoperation demo page.. when run it should connet we should be able to
  navigate round first, lets do only navigation. later we'll do the cmaera
  transfer, later, we'll do mapping.. lets do the plannoing brainstorming and
  start working on it." — the entire robium catalog stays dark for this
  session: only `superpowers:brainstorming`/`writing-plans`/`executing-plans`
  fire, and the one attempt to reach a robium skill fails outright — turns
  4076-4085 show `[TOOL_USE] Skill {"skill": "skill-updater"}` returning
  `<tool_use_error>Unknown skill: skill-updater</tool_use_error>` once, after
  which the assistant locates `skills/skill-updater/SKILL.md` on disk and
  follows its instructions **by hand** rather than through a real skill
  invocation.
  root-cause: the robium plugin was not loaded in this session — this is a
  session-bootstrap/environment condition, not a trigger-surface defect in
  any skill's description. (Originally mis-signed `[architect]
  no-skill-fired` with a "strengthen the description" fix framing; corrected
  after review — see lrn-0802-02 and the cross-session recurrence below.)
  fix: none applied this run (this is a process/setup finding, not a content
  bug in any skill) — check: none yet; the fix belongs in session-bootstrap
  tooling/docs (e.g. a hook or doctor check that fails loudly when
  `robium:*` isn't resolvable), a human decision, not a skill edit.
  dead-ends: none observed
  anchors: none (no skill's content is at fault)
  source: transcript robium-applications__20531984-b400-49be-a4c8-0c806bb6517d.jsonl#a8229e83-c09f-49cd-987b-117e03bb18c5 (kickoff, 2026-07-24) and #db19b266 region turns 4076-4085 (skill-updater invocation error, same session)

- [none] no-skill-fired <!-- id: lrn-0802-02 -->
  symptom: independent session, three consecutive natural-language
  kickoff/pivot turns — "i think we can work on isaac lab skills now. want to
  do some project at the same time learn isaac lab.. what projects we can do,
  also how to do it.. i have my macbook air onmlu", then "I want to mostly do
  VLA work.. which platforms are better for VLA work or more modesl
  available. .want to test with lerobot, google robotics, PI models", then
  "okay still wanna use isaac but may be we can do some rl work then.. what
  RL works are popular" — again zero `robium:*` Skill/Task invocations for
  the entire session. The assistant's own turn 344 names the cause directly:
  "Those robium skills/agents (`robium-architect`, `architect`,
  `environments`, `testing`) **are not loaded in this session** — so I can't
  route through them as the repo prescribes... I'll log it as a learning."
  It substitutes ad hoc `ctx7` lookups (Isaac Lab docs, LeRobot docs, openpi
  docs) for what `architect` (routing) plus `data`/`huggingface` (VLA
  platform survey) would have done.
  root-cause: same as lrn-0802-01 — plugin not loaded, an environment/session
  condition, confirmed explicitly in-transcript by the assistant itself.
  This exact root cause was *already* captured and correctly dispositioned
  once before: `learnings/2026-07-26-isaac-go2.md:8` — "[none] robium plugin
  not loaded this session... Proceeded manually... Surfaced to the user
  rather than silently substituting" — with its own footer (line 228) filing
  it as "kickoff-without-plugin session-setup gap (environment, not skill
  content)". Counting that entry, this is the third independently-documented
  occurrence of the same root cause (proof recorded at the observation, not
  here — see `learnings/observations/new-skills.md` obs-new-skills-001).
  fix: none applied this run — check: pending; same category of fix as
  lrn-0802-01 (session-bootstrap detection, not a skill edit)
  dead-ends: none observed
  anchors: none (no skill's content is at fault)
  source: transcript robium-applications__ac1f31b9-2387-453c-957a-34f9831b828b.jsonl#8bf5b495-930a-42ec-97ad-427d23d6f9a9 (2026-07-27) and turn 344 (same file, explicit plugin-not-loaded statement); cross-ref learnings/2026-07-26-isaac-go2.md:8,228

- [environments] user-correction <!-- id: lrn-0802-03 -->
  symptom: on a RunPod pod that was merely slow to cold-boot (20 GB Isaac Lab
  image pulling on a fresh host; proxy-exec round-trips stall 15-20s per
  attempt while the container isn't exec-ready yet), the assistant inferred
  "the container start is genuinely hung" from indirect signals (no
  "success" line in the start log, RunPod console showing "Jupyter Lab...
  taking longer than expected") and terminated the pod, then repeated the
  same guess-and-terminate pattern a second time. User corrected: "no before
  doing it lets try to undersand.. this is your second time terminating the
  pod." The assistant then admitted: "I don't actually have proof it was
  hung — it may have just been slow to start a 20 GB image on a fresh host,
  and I terminated it while impatient. That's the mistake you caught."
  root-cause: treated proxy-timing artifacts (proxy-exec round-trip stalls,
  RunPod console's port-8888-not-listening "Initializing..." message) as
  proof of a hung container instead of ground truth. RunPod's own Web
  Terminal (independent of the pod's entrypoint/sshd config) was available
  the whole time and was not used first.
  fix: stop guessing from indirect timing signals; use RunPod's Web Terminal
  (or another ground-truth channel) to directly confirm GPU/container state
  before terminating or recreating a pod — check: user accepted this as the
  path forward ("Which do you want — you flip the web terminal and paste
  output, or I do one patient API test? I won't terminate or recreate
  anything without checking with you first."); no independent re-run
  observed yet this pass, so treat as ready-by-user-correction but keep
  watching for recurrence.
  dead-ends: assuming a hung container from (a) no "success" log line within
  ~18 min of a cold 20GB image pull, (b) RunPod console's Jupyter-init
  message (that message just means nothing listens on 8888 yet, unrelated
  to whether the app container itself is fine).
  anchors: environments (new — no existing RunPod anchor covers premature
  pod termination; `references/gpu-cloud.md`'s "RunPod networking" section
  covers proxy-deadness/exposure/file-transfer but not this)
  source: transcript robium-applications-apps-go2-locomotion__1fb04329-8e55-49d6-8b1f-59a0f4600bae.jsonl#a4b1c0b6-6957-4f21-bc36-73d9998fd585 (apps/go2-locomotion, 2026-07-27)

- [none] user-correction <!-- id: lrn-0802-04 -->
  symptom: during the learning-engine design spec review, the user asked
  "Why did you miss such an impotrant feature. Can you review your reasoning
  again, why did you miss it? I wonder if there are similar impotant
  features in those finding we're not implemeting" after the assistant had
  compressed seven research reports into a design and dropped
  "transcripts-as-substrate" (present in five of seven reports) out of the
  spec.
  root-cause (assistant's own post-mortem, in-transcript): anchored on the
  incumbent `learnings/` file substrate instead of asking "what should the
  record of truth be" from zero; the synthesis step promoted a few headline
  principles and left load-bearing per-repo detail behind — "that is
  literally the failure mode the research warns about (summarization losing
  load-bearing content ... applied to my own synthesis)"; never ran a
  completeness pass (reports -> mechanism checklist -> coverage check against
  the spec).
  fix: ran a full audit, mechanism-by-mechanism, of all seven source reports
  against the spec, and folded in the misses (recall hook, transcripts as
  Tier -1, success-attribution counters, etc.) — check: the resulting spec
  commit (`docs: learning-engine spec — transcripts become Tier -1, hooks
  flag instead of copy`) is the artifact this repo's Phase 1/2 work was built
  from; no formal regression test, evidence is the shipped spec + this
  engine's own Phase 1/2a/2b task chain executing against it.
  dead-ends: none named
  anchors: none — this is an agent-methodology lesson (research synthesis
  should audit against a completeness checklist, not just a principles list)
  rather than a fact any current robium tool/umbrella skill owns; no
  catalog skill target identified, so no observation drafted for it (see
  task report — user's cross-session memory already carries an equivalent
  note, "research-completeness-pass").
  source: transcript robium__bbf8b546-6948-478d-b132-43c42ffadce9.jsonl#e800ccaa-4f7d-43b0-b03d-2d23049077ca (docs/superpowers/specs/2026-08-01-learning-engine-design.md review, 2026-08-01)

- [live-demo] figured-out-from-scratch <!-- id: lrn-0802-05 -->
  symptom: user wants `tb4.robium.ai` to reach a robot's teleop console
  publicly, while keeping `robium.ai`'s DNS on Google Cloud DNS ("no we cant
  move it from google it's currently google dns.."). The assistant reasons
  through every "no-server" tunnel option (Cloudflare Tunnel, ngrok free,
  Tailscale) live, unaided by any robium skill (zero `robium:*` invocations
  this session — same session as lrn-0802-01) and correctly derives: "the
  'no-server' tunnels (Cloudflare, Tailscale, free ngrok) all require *their*
  DNS or *their* domain... Since Cloud DNS can only point a record at a
  public IP, and the Orin has none, you need a small public endpoint on your
  side that the Orin dials out to." Final recommendation: a free GCE
  e2-micro relay VM (Orin reverse-tunnels to it, VM terminates
  TLS+Google-login, `tb4.robium.ai` A-records to the VM) — filed as "real
  demo infrastructure, which per your repo conventions lives in
  robium-website."
  root-cause: no skill fired to supply this pattern (checked `live-demo`,
  `cloud-run`, and `environments/references/robot-networking.md` — none
  mention relay VMs, dial-out tunnels, or the
  Cloudflare/ngrok/Tailscale-forces-a-DNS-or-hostname-move constraint). Given
  this session's plugin was not loaded (lrn-0802-01, same session), the
  non-firing is very plausibly explained by that same environment condition
  rather than a `live-demo`/`cloud-run` trigger-surface gap specifically —
  recorded as `figured-out-from-scratch` (the content itself is the finding)
  rather than a second `no-skill-fired` claim, to avoid repeating the
  original mis-signing this run was corrected for.
  fix: not implemented in this session (design decision only, deferred to a
  `robium-website` build) — check: none; the pattern is architecturally
  reasoned but unbuilt and unverified. status stays tentative until an
  actual relay VM ships and a demo is reached through it.
  dead-ends: none — the constraint-elimination table (Cloudflare/ngrok/
  Tailscale each ruled out for a specific, named reason) is itself the
  useful artifact even without a working build yet.
  anchors: none yet — candidate for `live-demo` or `cloud-run` (see the
  task report for the placement discussion; this was a borderline
  promote/drop call, resolved as promote-but-tentative)
  source: transcript robium-applications__20531984-b400-49be-a4c8-0c806bb6517d.jsonl#4462855b-7c37-4dcd-8444-fcc9b4122b4b (2026-07-25)
