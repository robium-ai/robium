- [none] noise <!-- id: lrn-0823-04 -->
  symptom: The capture hook queued a `make smoke` error whose excerpt contained `fetch-apps: no apps checkout ...; keeping committed src/data/apps.json`, even though this is an intentional fallback warning and the website smoke subsequently passed.
  root-cause: Error capture classified warning text in successful build output as a command failure without preserving or checking the final exit status.
  fix: Treat a zero command exit as authoritative and suppress this known `fetch-apps` fallback warning from error flags — check: the same session's diffusion-policy media learning records the completed 27-page build and passing smoke assertions.
  dead-ends: Treating every line containing a missing optional path as a failed build.
  source: capture queue signature 39493d96ca06, session 01a02714-7d51-7601-9771-527502555b04

- [none] noise <!-- id: lrn-0823-05 -->
  symptom: The capture hook queued a compound read-only `cat` command as an error even though its excerpt is ordinary SKILL.md and REGISTRY.md content with no failure message.
  root-cause: The flag lacks exit-status or stderr evidence and appears to infer failure from a large/truncated multi-file output.
  fix: Require a nonzero exit status or explicit stderr evidence before classifying read-only inspection output as an error — check: the files were read successfully and used to produce the committed ACT ALOHA architecture brief.
  dead-ends: Promoting the registry text itself as an application failure.
  source: capture queue signature 4a27409a0ae0, session 01a02714-7d51-7601-9771-527502555b04
