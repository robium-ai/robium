- [none] user-correction <!-- id: lrn-0817-20 -->
  symptom: website and application work could proceed into deployment without a fresh, explicit deploy instruction
  fix: added a repository guardrail requiring the maintainer to say “deploy” in the current conversation before any deployment, cloud image build, or production mutation — check: both robium-website and robium-apps AGENTS.md now carry the rule, and this work block performed no deployment
  dead-ends: prior task approval was rejected as deployment authorization because approval of implementation scope is not approval to deploy
  source: capture queue, session 01a00ead-3e30-7eb2-b677-58ef55c31d34, 2026-08-17

- [none] figured-out-from-scratch <!-- id: lrn-0817-21 -->
  symptom: `make image` failed in Cloud Build with `Error: Skill mismatch: integration` after the site skill-fetch step silently produced an incomplete catalog from GitHub.
  root-cause: GitHub's unauthenticated raw-file requests were throttled independently after the directory listing succeeded; the fetch script accepted the partial response and overwrote the complete committed `skills.json`.
  fix: fetch remote skill files concurrently with timeouts and retain the committed catalog whenever the remote result is empty or shorter than the committed fallback. (check: robium-website commit `42463bc` records the completed fix after the failing build `b196992e-17a8-47be-80d9-1a64cb70a8a2`.)
  dead-ends: treating `integration` as a stale taxonomy name; it existed in the source catalog and the failure was caused by partial remote ingestion.
  source: capture queue, session 01a00ead-3e30-7eb2-b677-58ef55c31d34, 2026-08-17
