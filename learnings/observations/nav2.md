# nav2 — mined observations

## custom global planner plugin (rolling API): subclass nav2_core::GlobalPlanner, configure() takes nav2::LifecycleNode/TransformBuffer, createPlan() takes viapoints + cancel_checker <!-- id: obs-nav2-001 -->
status: absorbed 2026-08-02
proof: 1
signal: better-method
sources: [ros-navigation/navigation2_tutorials@050a2d6]
target: nav2 (new-section) — add a "author a custom planner plugin" usage pattern, cross-referencing `references/nav2-architecture.md`'s "The task servers" planner-server bullet (which names plugin examples — NavfnPlanner, SmacPlanner family — but never shows how a plugin is written or registered): a global-planner plugin subclasses `nav2_core::GlobalPlanner` and implements `configure(parent, name, tf, costmap_ros)` / `cleanup()` / `activate()` / `deactivate()` / `createPlan(start, goal, viapoints, cancel_checker)`; `configure()`'s `costmap_ros` arg is how the plugin gets the costmap the planner-server section already documents (`costmap_ros->getCostmap()`, `costmap_ros->getGlobalFrameID()` — same file, not separately cited). Registration has three moving pieces, but only two are load-bearing in ROS 2 — **correction note: an earlier draft of this observation claimed all three parts were "required together" and that dropping any one left the plugin "not loadable by name from a params file"; that claim was unsupported by the cited code and is wrong, corrected here.** (1) `#include "pluginlib/class_list_macros.hpp"` + `PLUGINLIB_EXPORT_CLASS(nav2_straightline_planner::StraightLine, nav2_core::GlobalPlanner)` at the bottom of the .cpp (straight_line_planner.cpp#L158, same file, not separately cited) registers the concrete class with pluginlib's class-list-macro system. (2) the CMakeLists.txt's `pluginlib_export_plugin_description_file(nav2_core global_planner_plugin.xml)` call (CMakeLists.txt#L81, not separately cited) is the actual ROS 2 discovery mechanism: it installs `global_planner_plugin.xml` (a small XML file naming the class and its `base_class_type`) into the package's share dir AND writes the ament-index resource marker that pluginlib's `ClassLoader` queries at runtime to find plugins for a given base class — (1) and (2) together are what make the plugin loadable by name. (3) `package.xml`'s `<export><nav2_core plugin="${prefix}/global_planner_plugin.xml" /></export>` block (package.xml#L33, not separately cited) points at the same XML file but is a ROS 1 carryover this tutorial package still ships and is NOT load-bearing in ROS 2 — WebSearch synthesis of ROS 2's own `ament_cmake`/pluginlib documentation states plugin exporting in ROS 2 is done via CMakeLists.txt and "you don't have to export anything in your package.xml file"; ROS 1 is where the package.xml export tag did the work. Distro caveat (binding): mined from the `rolling` branch (2026-08-02 @ 050a2d6) — this repo has no Jazzy/Humble/Iron branch at all, `rolling` is its only branch. The `nav2::LifecycleNode::WeakPtr`/`nav2::TransformBuffer::SharedPtr` parameter types (from `nav2_ros_common`) and the `viapoints`/`cancel_checker` params on `createPlan` are recent additions — WebSearch synthesis of `docs.nav2.org`'s live tutorial plus `ros-navigation/navigation2` PR #1633 independently describes the same 4-parameter `createPlan` signature, so this is the current upstream-documented shape, not stale tutorial code, but the exact Jazzy-era `nav2_core::GlobalPlanner` signature (this skill's target distro) could not be directly confirmed — `docs.ros.org/en/jazzy/p/nav2_core/generated/classnav2__core_1_1GlobalPlanner.html` returned an Anubis "Access Denied" block on direct fetch. Re-verify the exact parameter list against Jazzy before absorbing this as a code snippet rather than a conceptual pattern.
evidence: official repo, consistent with current (rolling) docs (WebSearch synthesis 2026-08-02 — direct fetch of `docs.nav2.org/plugin_tutorials/docs/writing_new_nav2planner_plugin.html` reached the page (not blocked, not 404) but WebFetch's HTML→markdown conversion returned only the left-nav/table-of-contents, not the article body, so the createPlan/configure code itself came from WebSearch snippets of that same URL plus `navigation2` PR #1633's diff, both independently describing the identical viapoints+cancel_checker signature; a separate WebSearch on the pluginlib/package.xml discovery-mechanism claim independently corroborated via `docs.ros.org`'s Ament-CMake-Documentation pages (Humble/Jazzy/Rolling) and a `ros/pluginlib` GitHub issue thread; re-verify via a body-rendering fetch method on absorb, and separately re-verify against Jazzy specifically per the distro caveat above)
origin: external
source: ros-navigation/navigation2_tutorials@050a2d6 nav2_straightline_planner/src/straight_line_planner.cpp#L89-L93
quote: const std::vector<geometry_msgs::msg::PoseStamped> & viapoints,

## custom costmap layer plugin (rolling API): subclass nav2_costmap_2d::Layer, implement onInitialize/updateBounds/updateCosts, export via pluginlib_export_plugin_description_file <!-- id: obs-nav2-002 -->
status: absorbed 2026-08-02
proof: 1
signal: better-method
sources: [ros-navigation/navigation2_tutorials@050a2d6]
target: nav2 (new-section) — add an "author a custom costmap layer plugin" usage pattern, cross-referencing `references/nav2-architecture.md`'s "Costmap 2D" section (which names the shared layer *types* — `StaticLayer`, `ObstacleLayer`, `VoxelLayer`, `InflationLayer` — but never shows how a new layer type is written): a costmap layer plugin subclasses `nav2_costmap_2d::Layer` and overrides `onInitialize()` (parameter declaration + one-time state), `updateBounds(robot_x, robot_y, robot_yaw, min_x, min_y, max_x, max_y)` (grows the costmap's dirty-bounds window), `updateCosts(master_grid, min_i, min_j, max_i, max_j)` (writes cost values into that window), plus `reset()`, `onFootprintChanged()`, and `isClearable()`. Registration mirrors the planner-plugin pattern's load-bearing half only — see the correction note on obs-nav2-001: the package.xml export tag is not load-bearing in ROS 2 for either plugin type, so "mirrors one-for-one" (an earlier draft's phrasing) overstates the symmetry; corrected here. `PLUGINLIB_EXPORT_CLASS(nav2_gradient_costmap_plugin::GradientLayer, nav2_costmap_2d::Layer)` at the bottom of the .cpp (gradient_layer.cpp#L191, not separately cited) pairs with CMakeLists.txt's `pluginlib_export_plugin_description_file(nav2_costmap_2d gradient_layer.xml)` (CMakeLists.txt#L50, not separately cited) — this macro's first argument is pluginlib's resource-index key for the ament-index lookup (here `nav2_costmap_2d` rather than `nav2_core`); it is **not** something a params file's `plugins:` list names directly (an earlier draft claimed it "must match what the params file's `plugins:` list expects to load" — unsupported, dropped here). Worth flagging as its own small data point: package.xml's own (non-load-bearing) export tag doesn't even use the same string as the CMake macro in this package — `<costmap_2d plugin="${prefix}/gradient_layer.xml" />` (package.xml#L20, not separately cited) says `costmap_2d`, not `nav2_costmap_2d`, whereas the planner-plugin package's package.xml tag and CMake macro both say `nav2_core` identically. That asymmetry is itself circumstantial evidence the package.xml tag is vestigial — if it were load-bearing, a mismatched key here would break plugin loading, and nothing suggests this package is broken. Distro caveat: mined from the `rolling` branch (2026-08-02 @ 050a2d6, this repo's only branch) — the `Layer` base class's virtual-method set has been stable across recent distros per WebSearch synthesis (no evidence of a Jazzy-specific signature difference found this pass), but re-verify before absorbing verbatim per this skill's own no-param-facts-from-memory directive.
evidence: official repo, consistent with current (rolling) docs (WebSearch synthesis 2026-08-02 — direct fetch of `docs.nav2.org/plugin_tutorials/docs/writing_new_costmap2d_plugin.html` reached the page but again returned only nav/ToC content, not the article body; WebSearch snippets of that same URL independently describe the same three methods — onInitialize, updateBounds, updateCosts — as "the three core methods for a Costmap2D plugin", matching the cited header; re-verify via a body-rendering fetch method on absorb)
origin: external
source: ros-navigation/navigation2_tutorials@050a2d6 nav2_gradient_costmap_plugin/include/nav2_gradient_costmap_plugin/gradient_layer.hpp#L53-L58
quote: class GradientLayer : public nav2_costmap_2d::Layer
{
public:
  GradientLayer();

  virtual void onInitialize();

## nav2_core::Controller::computeVelocityCommands() always returns TwistStamped — the plugin-API return type is a separate fact from what nav2 actually publishes on /cmd_vel <!-- id: obs-nav2-003 -->
status: absorbed 2026-08-02
proof: 1
signal: better-method
sources: [ros-navigation/navigation2_tutorials@050a2d6]
target: nav2 (new-section, adjacent to `cmd-vel-twiststamped`) — **re-classified: an earlier draft of this observation used `signal: verified` and targeted `nav2#cmd-vel-twiststamped (annotate)`, framing this as a second confirmation of that anchor. That mis-classified the finding — corrected here.** The existing `cmd-vel-twiststamped` anchor is a wire-topic-level fact: whether `/cmd_vel` is published as `Twist` or `TwistStamped` is a runtime choice toggled by `enable_stamped_cmd_vel` across `controller_server`/`velocity_smoother`/`behavior_server`/`collision_monitor`/`docking_server`. This tutorial's controller plugin shows a DIFFERENT, decoupled fact one layer down: the `nav2_core::Controller` plugin interface's `computeVelocityCommands()` — the method `controller_server` calls every control cycle to get a command from the plugin — is declared returning `geometry_msgs::msg::TwistStamped` unconditionally in the cited header, with `override` against the rolling-branch `nav2_core::Controller` base class (a mismatched override wouldn't compile, so this confirms the base class currently declares this exact 5-parameter, `TwistStamped`-returning method as the virtual to implement, not just what this one plugin happens to do). Do NOT read this as explaining or confirming the anchor's `enable_stamped_cmd_vel` wire-format toggle: `controller_server` receives a `TwistStamped` from the plugin either way and is free to convert/republish it as a plain `Twist` on `/cmd_vel` depending on that separate parameter — a plugin always returning `TwistStamped` does not by itself determine the topic's wire type, and the two facts must not be conflated ("plugin returns TwistStamped ⇒ /cmd_vel is TwistStamped" is not a valid inference). Worth adding as a short adjacent note — new content, not an update to the anchor's existing guidance — specifically to head off that false inference for a future reader. Ambiguity note, tightened: a separate direct fetch of a cached `api.nav2.org` rolling doxygen page for `controller.hpp` showed an older 3-parameter `computeVelocityCommands(pose, velocity, goal_checker)` overload at that URL (no `transformed_global_plan`/`global_goal`). Given the compiling `override` in this tutorial's own rolling-branch code against the 5-parameter form, a stale/un-regenerated doxygen build at that cached URL is the more likely explanation than two currently-valid signatures — treat the cited tutorial's 5-parameter form as current, not as one of two live options (an earlier draft presented both as possibly-valid; corrected here). Distro caveat: mined from the `rolling` branch (2026-08-02 @ 050a2d6, this repo's only branch); Jazzy's exact `nav2_core::Controller` signature could not be directly confirmed — `docs.ros.org/en/jazzy/p/nav2_core/generated/classnav2__core_1_1Controller.html` returned an Anubis "Access Denied" block on direct fetch.
evidence: official repo, consistent with current (rolling) docs (direct fetch 2026-08-02 of `api.nav2.org/nav2-rolling/html/nav2__core_2include_2nav2__core_2controller_8hpp_source.html` — full body rendered, not just nav/ToC — independently shows a `computeVelocityCommands` overload returning `TwistStamped` in the upstream `nav2_core` base class itself, corroborating the cited tutorial file, though at an older 3-parameter arity per the ambiguity note above; the Jazzy-specific check separately failed per the distro caveat, re-verify against Jazzy on absorb)
origin: external
source: ros-navigation/navigation2_tutorials@050a2d6 nav2_pure_pursuit_controller/include/nav2_pure_pursuit_controller/pure_pursuit_controller.hpp#L43-L48
quote: geometry_msgs::msg::TwistStamped computeVelocityCommands(

## sam_bot_description bringup: robot_localization's ekf_filter_node fuses odom+IMU into odom→base_link TF; use_sim_time propagation in the same launch file is only partially consistent <!-- id: obs-nav2-004 -->
status: ready
proof: 1
signal: verified
sources: [ros-navigation/navigation2_tutorials@050a2d6]
target: nav2#use-sim-time-consistency (annotate) — **correction note: an earlier draft of this observation described this file as "one launch arg, every node agrees" and framed it as a clean positive exemplar of the anchor's guidance. That is contradicted by the cited file itself and is wrong — corrected here.** A `DeclareLaunchArgument(name='use_sim_time', default_value='True', ...)` (display.launch.py#L83, not separately cited) IS threaded as `LaunchConfiguration('use_sim_time')` into `robot_state_publisher_node`'s parameters (display.launch.py#L25, not separately cited) and into the `robot_localization` `ekf_node` (cited lines) — but two other nodes in the SAME file diverge: `camera_bridge_image` and `camera_bridge_depth` hardcode a literal `{'use_sim_time': True}` instead of using the `LaunchConfiguration` (display.launch.py#L52 and #L60, not separately cited — currently matches the arg's `True` default but would silently drift if the file were ever launched with `use_sim_time:=false`), and `rviz_node` (display.launch.py#L27-L33) sets no `use_sim_time` parameter at all (lower stakes for a pure visualizer, but still an omission). Corrected framing: this is worth citing as a real, official-repo instance of the exact inconsistency the anchor already warns against — even upstream tutorial code gets `use_sim_time` propagation only partly right — which strengthens the case for the anchor's warning rather than illustrating a clean best-practice to copy verbatim. Second, narrower finding, scoped to respect this skill's own existing boundary: `references/common-failures.md`'s step-2 TF-tree bullet (lines 66-69) already names `robot_localization` as a common `odom`→`base_link` source when fused with an IMU, and explicitly scopes the fusion setup itself to the `ros2` skill ("that fusion setup is the ros2/robot-description side, not this skill's") — **this is NOT a gap; an earlier draft of this observation incorrectly claimed the skill "never mentions robot_localization for odom→base_link" and proposed a new TF-chain note in `nav2-architecture.md` on that false premise. That claim is dropped here.** The only residual, in-scope increment this example actually adds: `robot_localization`'s own two-key contract for how it provides that transform, concretely instantiating the existing bullet's abstract claim rather than adding a new one — `ekf.yaml`'s `publish_tf: true` turns the TF broadcast on, and `world_frame: odom` (same file, not separately cited) tells the EKF to output relative to `odom` rather than `map`, the documented default for fusing continuous/relative sources (wheel odometry + IMU) as opposed to a discrete/global source like GPS. Worth a one-clause addition to the existing common-failures.md bullet if this skill wants the concrete key names (no anchor currently exists on that bullet to annotate formally), not a new section. Distro caveat: mined from the `rolling` branch (2026-08-02 @ 050a2d6, this repo's only branch); `robot_localization` itself is not a nav2-owned package and its EKF config surface is not known to be nav2-distro-coupled, but re-verify the setup-guide content directly before absorbing verbatim.
evidence: official repo, illustrates existing skill anchor (as a cautionary/partial example — see correction note above, not a clean confirmation) (direct fetch 2026-08-02 of `docs.nav2.org/setup_guides/odom/setup_robot_localization.html` — **correction note: an earlier draft of this observation cited `docs.nav2.org/setup_guides/urdf/setup_urdf.html` and claimed a direct fetch of that page confirmed a "Smoothing Odometry using Robot Localization" section within it; that was wrong — that heading is a sibling page's title (this corrected URL), surfaced via the left-nav table-of-contents, not a section of setup_urdf.html itself. Corrected here.** The corrected URL's direct fetch reached the page (200, not blocked, not 404) but again rendered only nav/ToC content, not the article body — so the `publish_tf`/`world_frame` claim above is corroborated only by WebSearch snippet-synthesis of that same corrected URL ("Robot_localization can also publish the odom => base_link transform on the /tf topic"), not a direct-fetch body match; re-verify via a body-rendering fetch method on absorb)
origin: external
source: ros-navigation/navigation2_tutorials@050a2d6 sam_bot_description/launch/display.launch.py#L72-L78
quote: parameters=[os.path.join(pkg_share, 'config/ekf.yaml'), {'use_sim_time': LaunchConfiguration('use_sim_time')}],
