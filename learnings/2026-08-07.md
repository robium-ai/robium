# 2026-08-07 — Codex-native integration audit

Queue consolidation reviewed all 44 pending flags. Flags 1–32 were stale
copies from the 2026-08-02 acceptance run and had already been promoted or
discarded there. Flags 33–38 were user preferences or UI/task-local choices
without a reusable robotics-skill finding. Flags 39–44 were hook noise: a
bare stop request and successful/read-only command output misclassified as
errors. The reusable findings from this work block are recorded below.

- [none] figured-out-from-scratch <!-- id: lrn-0807-01 -->
  symptom: after installing the native Codex plugin, every `robium:*` skill
  appeared twice in Codex discovery.
  root-cause: the checkout exposed the same 24 skills through two independent
  discovery sources: the installed `robium@robium` plugin and a committed
  repo-scoped `.agents/skills/` symlink farm.
  fix: removed the repo-scoped farm and its checker/bootstrap path; Codex now
  gets Robium skills from the native plugin only, while other supported agents
  retain their own explicit setup paths — check: `.agents/skills` is absent,
  the plugin validator passes, all 24 skills validate, and `codex plugin list
  --marketplace robium --json` reports `robium@robium` 0.2.2 installed and
  enabled.
  dead-ends: keeping the symlink farm as a cross-agent compatibility layer;
  it is valid in isolation but duplicates every skill when the native Codex
  plugin is also installed.
  anchors: none (Codex plugin/setup behavior, fixed in plugin and CLI files)
  source: Codex task 019fdcd0-419d-7df2-bcbd-ffb852054735 (robium, 2026-08-07)

- [none] figured-out-from-scratch <!-- id: lrn-0807-02 -->
  symptom: the active Codex task emitted `can't open file .../robium/0.2.1/
  hooks/scripts/stop_nudge.py` after Robium had been upgraded to plugin 0.2.2.
  root-cause: a running task retains the hook paths resolved from the plugin
  version active when that task started; upgrading the plugin does not rewrite
  those in-memory paths, and the old 0.2.1 cache entry lacked its hook scripts.
  fix: installed 0.2.2 with the complete hook bundle and added temporary
  forwarding shims at the exact cached 0.2.1 paths for the already-running
  task — check: Stop and PostToolUse shims executed successfully, the updated
  hook tests pass, and the installed plugin reports 0.2.2 enabled.
  dead-ends: reinstalling 0.2.2 alone; it fixes new tasks but cannot change
  hook paths already held by the current task.
  anchors: none (Codex plugin lifecycle behavior, not robotics skill content)
  source: Codex task 019fdcd0-419d-7df2-bcbd-ffb852054735 (robium, 2026-08-07)

## End-of-block retro

- `learning-loop` — fired: yes; accurate: yes; complete: yes; lean: yes. It
  correctly kept consolidation outside `skills/**`, deduplicated the stale
  queue against the prior acceptance run, and retained only reusable findings.
