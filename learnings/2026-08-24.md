- [skill-author] figured-out-from-scratch <!-- id: lrn-0824-30 -->
  symptom: The requested generic `skill-creator` validator first failed with `ModuleNotFoundError: No module named 'yaml'`, then rejected the committed Robium skill with `Unexpected key(s) in SKILL.md frontmatter: version`.
  root-cause: The standalone validator does not declare its PyYAML runtime dependency and its generic frontmatter schema does not include Robium's mandatory `version` extension.
  fix: Run the generic validator through `uv run --with pyyaml` against a disposable copy with only the Robium-specific `version` field removed, while validating the unchanged committed skill with Robium's authoritative validator — check: generic validation printed `Skill is valid!` and the repository validator printed `Checked 25 skills: PASS`.
  dead-ends: Removing `version` from the real Robium skill; installing PyYAML into system Python; treating either validator as authoritative for the other format.
  anchors: skill-author#wrap-skill-creator-for-evals-not-reinvent, skill-author#always-run-validator-before-commit
  source: generic skill-creator initialization/validation and Robium runpod-skill authoring on 2026-08-24.

- skill-author — fired: yes; accurate: yes, it enforced the Robium template, ownership placement, archives, version bumps, references, and 25-skill validator; complete: no, the generic skill-creator validator's frontmatter/runtime incompatibility required live discovery captured above; lean: yes.

- [none] figured-out-from-scratch <!-- id: lrn-0824-31 -->
  symptom: A zsh archive-integrity loop assigned to `path`, after which `git`, `wc`, `tr`, and `find` all returned `command not found`; a retry using `set -- $spec` then failed because zsh did not apply Bash-style word splitting.
  root-cause: In zsh, lowercase `path` is a special array tied to `PATH`, and unquoted scalar expansion does not split words unless `SH_WORD_SPLIT` is enabled.
  fix: Use a task-specific variable such as `repo_file` and call a validation function with explicit arguments — check: all four required skill archives matched the pre-edit Git blobs and file counts exactly.
  dead-ends: Reusing `path` as a harmless loop variable; parsing `"skill version"` pairs through implicit word splitting; trusting output from a check whose own commands failed.
  source: archive-integrity validation during RunPod skill authoring on 2026-08-24.
