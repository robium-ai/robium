# npm auth via Doppler-injected env var; no secret here.
# Publish with:  cd cli && doppler run -- npm publish
# (npm expands ${NPM_TOKEN} from the environment at runtime.)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
