triggers:
  positive:
    - phrase: the ACT app is tested; publish its overview, live page, guide, social card, and system diagram in the Robium style
    - phrase: rewrite this finished app article so it sounds human, uses the product-lab voice, and shares a theme with its demo
  negative:
    - phrase: brainstorm a new robot application and choose the simulator, model, robot, and visualization
      expect: architect
    - phrase: the published project page exists, but its gateway claim and Cloud Run session teardown are broken
      expect: live-demo
tasks: []
