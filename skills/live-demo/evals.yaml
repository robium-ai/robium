triggers:
  positive:
    - phrase: the robot app works and smoke is green; add private Cloud Run instances with start, status, boot logs, gateway claims, and stop
    - phrase: add a RunPod-backed ten-minute policy session with a hard lifetime, daily budget, and reliable delete
  negative:
    - phrase: the application is verified; publish its overview, catalog card, article, and shared project navigation
      expect: app-publishing
    - phrase: brainstorm a new mobile robot app and choose its stack before implementation
      expect: architect
tasks: []
