triggers:
  positive:
    - phrase: should this Python robotics project use uv or Docker
    - phrase: make the local and remote CUDA environments reproducible
  negative:
    - phrase: find an available RunPod GPU in the same region as my volume
      expect: runpod
tasks: []
